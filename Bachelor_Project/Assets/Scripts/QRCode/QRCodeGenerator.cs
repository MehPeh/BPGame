using System;
using System.IO;

public class QRCodeGenerator
{
      /*
      public static void GenerateQRCode()
      {
            string ip4Address = IPAddressFinder.GetLocalIPv4();
            string function = "= IMAGE ( \"https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=http://" + ip4Address + ":3000/.html/index.html\" )";
            string filePath = Path.Combine(Directory.GetCurrentDirectory(), "QRCode.xlsx");

            ExcelWriter.WriteTextToExcel(function, filePath);
            Console.WriteLine("IPv4 Address written to Excel file: " + ipv4Address);
      }
      */
}